# UI refresh notes

This refresh keeps all map and chat behavior in the existing standalone frontend while improving visual hierarchy, multi-layer map exploration, selected-point context, and explicit source/status indicators. No frontend framework or additional runtime dependency is introduced.
